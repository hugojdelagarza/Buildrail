# Copy this file to .env and fill in real values locally.
# .env is git-ignored — never commit real secrets, keys, or tokens.
#
# Required only when buildrail.toml sets provider = "anthropic". Never
# read from buildrail.toml itself — the key must come from the
# environment. Not needed for provider = "fake".

# ANTHROPIC_API_KEY=
