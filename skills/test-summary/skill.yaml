name: test-summary
version: 0.1.0
protocol_version: "1.0"
description: Runs the project's test suite and summarizes any failures.
entrypoint: "python skill.py"

inputs: []

outputs:
  - name: summary
    artifact_type: test-summary

requires_provider: true
provider_capabilities_required:
  - text-completion
