{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Minimal capabilities for the Buildrail desktop shell: window basics only. No filesystem, shell, dialog, or HTTP plugins are granted — the WebView's own fetch already reaches the local Buildrail service, restricted by the CSP connect-src in tauri.conf.json.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
